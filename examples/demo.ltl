// examples/demo.ltl — runnable demo for ltl_nginx_parse

import std::io
use ltl_nginx_parse as lib

fn main() {
    io::println("ltl_nginx_parse demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}